name: Build ImmortalWrt

on:
  push:
    branches: [main]
  workflow_dispatch:

permissions:
  contents: write
  packages: read

jobs:
  build:
    runs-on: ubuntu-22.04
    env:
      DEBIAN_FRONTEND: noninteractive

    steps:
      - name: Checkout repository
        uses: actions/checkout@v3

      - name: Install build dependencies
        run: |
          chmod +x ./install-env.sh
          sudo ./install-env.sh

      - name: Setup Python
        uses: actions/setup-python@v4
        with:
          python-version: 3.11

      - name: Build ImmortalWrt
        run: |
          chmod +x ./build-immortalwrt.sh
          ./build-immortalwrt.sh

      - name: Generate snapshot version with config state
        id: snapshot
        run: |
          SNAPSHOT_DATE=$(date "+%Y%m%d%H%M")
          SNAPSHOT_TAG="R${SNAPSHOT_DATE}"
          echo "snapshot_tag=${SNAPSHOT_TAG}" >> $GITHUB_OUTPUT
          echo "Snapshot tag: ${SNAPSHOT_TAG}"

      - name: Generate changelog
        id: changelog
        run: |
          mkdir -p logs
          RELEASE_LOG="logs/release_notes.txt"
          echo "ImmortalWrt Build ${{ steps.snapshot.outputs.snapshot_tag }}" > $RELEASE_LOG
          echo "Build date: $(date)" >> $RELEASE_LOG
          echo "" >> $RELEASE_LOG
          echo "Generated files:" >> $RELEASE_LOG
          ls -lh immortalwrt-* >> $RELEASE_LOG
          echo "" >> $RELEASE_LOG
          echo "SHA256 sums:" >> $RELEASE_LOG
          cat sha256sums >> $RELEASE_LOG

      - name: Upload artifacts
        uses: actions/upload-artifact@v4
        with:
          name: immortalwrt-artifacts
          path: |
            immortalwrt-*
            profiles.json
            sha256sums
            logs/release_notes.txt
            config.buildinfo
            feeds.buildinfo
            *.manifest
            *-squashfs-sysupgrade.img.gz

      - name: Create GitHub Release
        id: create_release
        uses: softprops/action-gh-release@v1
        with:
          tag_name: ${{ steps.snapshot.outputs.snapshot_tag }}
          name: ImmortalWrt Build ${{ steps.snapshot.outputs.snapshot_tag }}
          body_path: logs/release_notes.txt
          draft: false
          prerelease: true
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

      - name: Upload Release Assets
        uses: softprops/action-gh-release@v1
        with:
          files: |
            immortalwrt-*
            profiles.json
            sha256sums
            logs/release_notes.txt
            config.buildinfo
            feeds.buildinfo
            *.manifest
            *-squashfs-sysupgrade.img.gz
